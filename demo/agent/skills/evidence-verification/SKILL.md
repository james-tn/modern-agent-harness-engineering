---
name: evidence-verification
description: Verify event-date alignment, calculations, provenance, policy compliance, chart integrity, and bounded completion.
license: MIT
metadata:
  version: "1.1"
---

# Evidence verification

The model proposes completion; the verifier decides.

Recompute key returns independently. Confirm event-date alignment, benchmark use, adjusted
prices, evidence IDs, required sections, chart data equality, restricted-field exclusion,
successful script execution, and explicit approval. Return concrete corrections and stop
after the configured iteration bound.
